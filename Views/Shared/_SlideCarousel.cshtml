@model Slide

@{
    // Solo imágenes activas y ordenadas
    var imagenes = Model.SlideImages
        .Where(i => i.Activo)
        .OrderBy(i => i.Orden)
        .ToList();
}

@if (imagenes.Any())
{
    <div id="carousel-@Model.SlideId"
         class="carousel slide w-100 h-100"
         data-bs-ride="carousel"
         data-bs-interval="3000">

        <div class="carousel-inner w-100 h-100">

            @for (int i = 0; i < imagenes.Count; i++)
            {
                <div class="carousel-item @(i == 0 ? "active" : "") w-100 h-100">

                    <div class="d-flex justify-content-center align-items-center w-100 h-100">
                        <img src="@imagenes[i].RutaImagen"
                             class="slide-img img-fluid"
                             style="max-width:100%; max-height:100%; object-fit:contain;" />
                    </div>

                </div>
            }

        </div>

    </div>
}